apply from: rootProject.file('gradle/grpc.gradle')

dependencies {
    compile project(':jood-api')

    compile "org.apache.kafka:kafka-clients:$versions.kafka"

    testCompile project(':jood-test')
    testCompile "org.apache.kafka:kafka_2.12:$versions.kafka"
}